$("ul.errors").html("")
<% if @alloted_time.errors.any? %>
  <% @alloted_time.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#alloted_time-modal").modal("hide")
  window.location.reload();
<% end %>
